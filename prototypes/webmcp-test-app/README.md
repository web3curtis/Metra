# ReliableRail Sandbox

Public WebMCP demo application (TypeScript + Vite). Simulated ticket workflow only.

```bash
cd prototypes/webmcp-test-app
npm install
npm run dev      # http://localhost:5173
npm test
npm run calibrate
```

Fixture source of truth: `configurations/fixtures/fixture-v0.json`.

Personal research dashboard is **not** part of this package; see repo `private/` (gitignored, unpublished).
